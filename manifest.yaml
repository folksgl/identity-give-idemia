---
version: 1
applications:
  - name: ipp-idemia
    random-route: true
    memory: ((memory))
    instances: ((instances))
    services:
      - ipp-idemia-db
    buildpacks:
      - python_buildpack
    env:
      SECRET_KEY: ((SECRET_KEY))
      DEBUG: False
